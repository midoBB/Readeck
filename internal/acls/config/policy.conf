# -------------------------------------------------------------------
# Roles
# -------------------------------------------------------------------

# System routes
p, system.read, system, read


# Admin
p, admin.read,  admin:users,     read
p, admin.read,  api:admin:users, read
p, admin.write, admin:users,     write
p, admin.write, api:admin:users, write


# Cookbook
p, cookbook.read, cookbook,     read
p, cookbook.read, api:cookbook, read


# User profile
p, profile.read,  api:profile, read
p, profile.read,  profile,     read
p, profile.write, api:profile, write
p, profile.write, profile,     write


# Tokens (not available to user for now)
p, tokens.read,  profile:tokens, read
p, tokens.write, profile:tokens, write


# Bookmarks
p, bookmarks.read,   bookmarks,     read
p, bookmarks.read,   api:bookmarks, read
p, bookmarks.write,  bookmarks,     write
p, bookmarks.write,  api:bookmarks, write
p, bookmarks.export, api:bookmarks, export

# Bookmark collections
p, bookmarks.collections.read,  bookmarks:collections,     read
p, bookmarks.collections.read,  api:bookmarks:collections, read
p, bookmarks.collections.write, bookmarks:collections,     write
p, bookmarks.collections.write, api:bookmarks:collections, write

# -------------------------------------------------------------------
# Groups
# -------------------------------------------------------------------

# Group "user"
g, user, profile.*
g, user, bookmarks.read
g, user, bookmarks.write
g, user, bookmarks.export
g, user, bookmarks.collections.read
g, user, bookmarks.collections.write

# Group "staff"
g, staff, user
g, staff, system.*
g, staff, tokens.*

# Group "admin"
g, admin, staff
g, admin, admin.*
g, admin, cookbook.*
